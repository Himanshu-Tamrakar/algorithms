package chapter3.section4.solutions;

/**
 * Fingerprinting. Hash function (e.g., MD5 and SHA-1) are also useful for verifying the integrity of a file.
 * Hash the file to a short string, transmit the string with the file, if the hash of the transmitted file differs from the hash value then the data was corrupted.
 */
public class FingerPrinting {
}
